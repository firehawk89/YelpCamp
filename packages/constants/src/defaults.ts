import { SortOrder } from '@repo/types';

export const DEFAULT_PAGE: number = 1;
export const DEFAULT_PAGE_LIMIT: number = 8;

export const DEFAULT_SORT_FIELD = 'createdAt';
export const DEFAULT_SORT_ORDER: SortOrder = 'desc';
