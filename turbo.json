{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"],
      "env": ["PORT", "DATABASE_*", "JWT_*", "CLOUDINARY_*"]
    },
    "lint": {
      "dependsOn": ["^lint"],
      "env": ["PORT", "DATABASE_*", "JWT_*", "CLOUDINARY_*"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "env": ["PORT", "DATABASE_*", "JWT_*", "CLOUDINARY_*"]
    }
  }
}
